#include <stdio.h>
void triParSelection(int tab[]);
int main(){
    int tab[5] = {5,9,7,2,8};
    triParSelection(tab);
    return 0;
}

void triParSelection(int tab[]){
    for(int i=0;i<5;i++){
        // printf("%d\n",tab[i]);
        
    }
}